apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: headscale
  namespace: headscale
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: headscale
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
