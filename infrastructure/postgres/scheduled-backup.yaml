apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: homelab-postgres-daily
  namespace: postgres
spec:
  schedule: "0 3 * * *"
  backupOwnerReference: self
  cluster:
    name: homelab-postgres
